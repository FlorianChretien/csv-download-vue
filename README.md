# csv-download-vue

Component VueJS for dowload json on csv. Based on json2csv.

### Lauch exemple

- Lauch `index.html`
- Change data in `index.js` and build it

### Start work

- yarn (for install)
- yarn run dev --watch 
